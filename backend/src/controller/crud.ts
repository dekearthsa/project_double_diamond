const {answerModel} = require("../model/modelAnswer");
const {userProfileModel} = require("../model/modelUserProfile");

export namespace HaddleCRUD {
    export class InsertData{
        async insertUserProfile() {

        }
    }
    
    export class FindData{
        async findingUserProfile() {

        }
    }
}